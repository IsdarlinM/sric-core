# Architecture

SRIC is a shared library/API core, not a microservice mesh. Consuming products import versioned schemas and services. Evidence and policy decisions remain deterministic boundaries outside AI providers.

Data lineage: raw artifact -> EvidenceReference -> Observation -> Claim/Hypothesis -> Validation -> Finding.
